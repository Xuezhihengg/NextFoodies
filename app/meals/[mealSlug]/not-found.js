export default function NotFoundPage() {
	return (
		<main className="not-found">
			<h1>MEAL NOT FOUND</h1>
			<p>Unfortunately , we could not find the requested page for meal data.</p>
		</main>
	);
}
